const cipher = {
  //El mensaje ingresado se guarda en una variable
  //1. Buscar para que sirve chartCodeAt() y String.charCodeAt()
  //2. Crear loop (for, por ejemplo) para ir iterando por cada letra del mensaje ingresado por el usuario
  //3. Utilizar fórmula dentro del loop (puedes utilizar un offset fijo de 5 para probar)
  //4. Guardar mensaje cifrado en una variable y retornarlo
}
export default cipher;